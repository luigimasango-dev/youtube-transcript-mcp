[project]
name = "youtube-transcript-mcp"
version = "0.1.0"
description = "MCP server exposing YouTube transcript fetch/search/summarize tools (no API key required)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.28.1",
    "requests>=2.34.2",
    "youtube-transcript-api>=1.2.4",
]
